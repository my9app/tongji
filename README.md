# 📊 LiteStats - 轻量级网站统计系统

整合了 Umami、Plausible、Ackee 等开源项目优点的轻量级、隐私友好的网站统计系统。

## ✨ 特性

- 🔐 **登录认证** - 用户名密码保护，防止未授权访问
- 🚀 **轻量级** - Docker 镜像小于 100MB，内存占用低
- 🔒 **隐私友好** - 无 Cookie，不存储用户 IP，符合 GDPR
- 📊 **实时统计** - PV、UV、在线用户实时更新
- 🌍 **地理位置** - 自动识别访客国家/城市
- 📱 **设备识别** - 浏览器、操作系统、设备类型
- 🔗 **来源追踪** - 记录流量来源
- 📈 **美观图表** - 趋势图、饼图、表格
- 🎯 **自定义事件** - 追踪按钮点击、表单提交等
- 🌐 **多站点支持** - 一个实例管理多个网站，支持分组
- 🐳 **一键部署** - Docker Compose 快速启动

## 🚀 一键安装

```bash
# 先切换到 root
sudo -i

# 一键安装（默认端口 8080，用户名 admin，密码 admin123）
bash <(curl -Ls https://raw.githubusercontent.com/my9app/tongji/main/install.sh)

# 自定义端口和密码（推荐）
bash <(curl -Ls https://raw.githubusercontent.com/my9app/tongji/main/install.sh) 8080 admin YourSecurePassword
```

### 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 第1个 | 端口 | 8080 |
| 第2个 | 用户名 | admin |
| 第3个 | 密码 | admin123 |

## 📖 使用指南

### 1. 登录系统

访问 `http://你的服务器IP:8080`，输入用户名密码登录。

### 2. 添加站点

登录后点击左侧「+ 添加站点」，填写：
- 站点名称（如：我的博客）
- 域名（如：blog.example.com）
- 分组（可选，如：生产环境、测试站点）
- 备注（可选）

### 3. 获取追踪代码

双击站点打开设置，复制追踪代码添加到网站的 `<head>` 中：

```html
<script>
window.LITESTATS_URL = 'http://your-server:8080';
window.LITESTATS_TOKEN = 'your-token-here';
</script>
<script src="http://your-server:8080/tracker.js"></script>
```

### 4. 追踪自定义事件

```javascript
// 追踪按钮点击
litestats.track('button_click', { button: 'signup' });

// 追踪表单提交
litestats.track('form_submit', { form: 'contact' });

// 追踪购买
litestats.track('purchase', { amount: 99.99, product: 'pro_plan' });
```

## 🔧 管理命令

```bash
# 查看日志
docker logs -f litestats

# 重启服务
cd /opt/litestats && docker compose restart

# 停止服务
docker stop litestats

# 修改密码
# 编辑 /opt/litestats/.env 文件，修改 ADMIN_PASS
# 然后重启: cd /opt/litestats && docker compose restart

# 备份数据
cp /opt/litestats/data/stats.db ~/backup/

# 卸载
docker rm -f litestats && rm -rf /opt/litestats
```

## ⚙️ 配置文件

配置文件位于 `/opt/litestats/.env`：

```env
PORT=8080
ADMIN_USER=admin
ADMIN_PASS=your-password
```

修改后需要重启服务生效。

## 📊 功能说明

### 多站点管理

- **分组功能**：站点可以按分组归类（如：生产环境、测试站点）
- **快速切换**：侧边栏显示所有站点，点击即可切换
- **今日数据**：侧边栏显示每个站点的今日 PV/UV

### 统计数据

- **总览**：所有站点今日汇总 PV/UV/在线数
- **趋势图**：PV/UV 随时间变化趋势
- **热门页面**：访问量最高的页面
- **流量来源**：访客从哪里来
- **设备分布**：桌面/移动/平板占比
- **浏览器分布**：Chrome/Safari/Firefox 等
- **国家分布**：访客地理位置
- **实时访问**：最近 30 分钟的访问记录

## 📝 许可证

MIT License
